{
  "buildCommand": "npm --prefix admin_web install && npm --prefix admin_web run build",
  "outputDirectory": "admin_web/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}

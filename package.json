{
  "scripts": {
    "dev:web": "npm --prefix admin_web run dev",
    "build:web": "npm --prefix admin_web run build",
    "preview:web": "npm --prefix admin_web run preview",
    "reset:storage": "node scripts/reset_production_storage.mjs"
  },
  "dependencies": {
    "@supabase/ssr": "^0.10.3",
    "@supabase/supabase-js": "^2.106.2"
  }
}
